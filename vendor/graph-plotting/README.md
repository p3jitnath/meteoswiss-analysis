# Graph-plotting runtime

CI downloads the open, portable plotting helper used by this project into this directory.
The repository's local rendering uses the installed Codex graph-plotting skill. The required
helper and Nimbus Sans assets are copied here during repository generation so GitHub Actions
can reproduce figures without a Codex installation.
